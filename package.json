{
  "name": "darwin-ui-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "run-sdk-on-demo": "cd packages/demo-app && npx ts-node --require ../darwin-sdk/polyfill.js ../darwin-sdk/cli/index.ts run"
  }
}
